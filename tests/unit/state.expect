{modes, [seq, reactive]}.
{cycles, 5}.
{checks, [
  %% 1 = NORMAL. INIT is a one-cycle state: nothing here moves State, so it
  %% steps out by itself after the first cycle (see init_once).
  {final_var, "State", 1},
  {final_var, "X", 0},
  {final_var, "Y", 0},
  {final_var, "Z", 0}
]}.
