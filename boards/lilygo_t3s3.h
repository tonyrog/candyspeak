// lilygo_t3s3 -- nothing board-specific yet; every setting falls through to
// the defaults in csp_config.h. The file exists so the target has somewhere
// to put its first delta.
#include "embedded.h"
