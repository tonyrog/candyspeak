// generated by utils/gen_patterns.erl from utils/syntax.terms -- do not edit
//
// Capture structs and the pattern bytes. Every byte length here was
// computed, not counted. See utils/syntax.terms for what each pattern
// means and why it is shaped the way it is.
//
// ONE array, and PAT_* indexes it. Eighteen separate arrays meant a table
// of POINTERS to resolve P_PAT through, and a pointer table cannot follow
// them into flash on AVR: ro_ptr is a 16-bit pgm_read_word, so it holds
// only as long as the linker keeps every pattern under 64K. An OFFSET has
// no such ceiling, so the table is RODATA like the bytes it indexes and
// nothing is registered at boot any more.
#ifndef __CSP_PATTERNS_H__
#define __CSP_PATTERNS_H__

#define MAX_BODY_PARTS 8
#define MAX_INITS 8
#define MAX_PACK MAX_BODY_PARTS

typedef struct {
    ivalue_t res;
} res_param_t;
typedef struct {
    ivalue_t port;
    ivalue_t pin;
} port_pin_t;
typedef struct {
    value_t init;
    decl_opts_t opts;
    ivalue_t sep;
} initval_param_t;
typedef struct {
    ivalue_t num;
    ivalue_t colon;
    ivalue_t lo;
    ivalue_t hi;
    ivalue_t sep;
} pin_item_t;
typedef struct {
    tstr_t name;
} module_param_t;
typedef struct {
} end_param_t;
typedef struct {
    tstr_t name;
    res_param_t r;
    decl_opts_t opts;
    value_t init;
    pexpr_t ini;
} variable_param_t;
typedef struct {
    tstr_t name;
    res_param_t r;
    decl_opts_t opts;
    value_t init;
    ivalue_t list;
} constant_param_t;
typedef struct {
    tstr_t name;
    port_pin_t port_pin;
    decl_opts_t opts;
} digital_param_t;
typedef struct {
    tstr_t name;
    res_param_t r;
    port_pin_t port_pin;
    decl_opts_t opts;
} analog_param_t;
typedef struct {
    tstr_t name;
    ivalue_t timeout;
    ivalue_t init;
    pexpr_t period;
} timer_param_t;
typedef struct {
    tstr_t name;
    res_param_t r;
    tstr_t frame;
    ivalue_t bit0;
    ivalue_t bit1;
    decl_opts_t opts;
} field_param_t;
typedef struct {
    tstr_t name;
    res_param_t r;
    decl_opts_t opts;
    ivalue_t frameid;
    uint8_t is_can;
    ivalue_t i2c_bus;
    ivalue_t i2c_addr;
    ivalue_t i2c_reg;
    ivalue_t spi_bus;
    port_pin_t spi_cs;
    ivalue_t spi_cmd;
    ivalue_t udp_ip;
    ivalue_t udp_port;
} buffer_param_t;
typedef struct {
    tstr_t obj;
    tstr_t fld;
    tstr_t pfld;
    ivalue_t assign;
    ivalue_t has_idx;
    ivalue_t has_range;
    int idx_bits;
    int is_unpack;
    index_t src_view;
    ivalue_t idx0;
    ivalue_t idx1;
    pexpr_t idxe;
    pexpr_t rhs;
} rule_body_part_t;
typedef struct {
    pexpr_t cond;
    rule_body_part_t body[MAX_BODY_PARTS];
} rule_param_t;
typedef struct {
    tstr_t mod_name;
    tstr_t obj_name;
    rule_body_part_t inits[MAX_INITS];
} object_param_t;
typedef struct {
    pexpr_t val;
    ivalue_t bits;
} pack_field_t;
typedef struct {
    pack_field_t field[MAX_PACK];
    tstr_t buffer;
    ivalue_t op;
    pexpr_t cond;
} pack_param_t;

extern const uint8_t csp_pattern_data[] RODATA;

// Where each pattern starts. A P_PAT carries one of these directly, so
// there is no id and no table to look one up in.
#define PATOFF_RES 0
#define PATOFF_PORT_PIN 9
#define PATOFF_INITVAL 35
#define PATOFF_PIN_ITEM 55
#define PATOFF_MODULE 107
#define PATOFF_END 110
#define PATOFF_VARIABLE 111
#define PATOFF_CONSTANT 142
#define PATOFF_DIGITAL 170
#define PATOFF_ANALOG 180
#define PATOFF_TIMER 195
#define PATOFF_FIELD_DECL 221
#define PATOFF_BUFFER 257
#define PATOFF_BODY 319
#define PATOFF_RULE 388
#define PATOFF_OBJECT 415
#define PATOFF_PACK_FIELD 434
#define PATOFF_PACK 446

// Each pattern as a pointer into the one array -- compile-time
// constants, so a pmatch call site reads as it did when these were
// separate arrays.
#define pat_res (csp_pattern_data + PATOFF_RES)
#define pat_port_pin (csp_pattern_data + PATOFF_PORT_PIN)
#define pat_initval (csp_pattern_data + PATOFF_INITVAL)
#define pat_pin_item (csp_pattern_data + PATOFF_PIN_ITEM)
#define pat_module (csp_pattern_data + PATOFF_MODULE)
#define pat_end (csp_pattern_data + PATOFF_END)
#define pat_variable (csp_pattern_data + PATOFF_VARIABLE)
#define pat_constant (csp_pattern_data + PATOFF_CONSTANT)
#define pat_digital (csp_pattern_data + PATOFF_DIGITAL)
#define pat_analog (csp_pattern_data + PATOFF_ANALOG)
#define pat_timer (csp_pattern_data + PATOFF_TIMER)
#define pat_field_decl (csp_pattern_data + PATOFF_FIELD_DECL)
#define pat_buffer (csp_pattern_data + PATOFF_BUFFER)
#define pat_body (csp_pattern_data + PATOFF_BODY)
#define pat_rule (csp_pattern_data + PATOFF_RULE)
#define pat_object (csp_pattern_data + PATOFF_OBJECT)
#define pat_pack_field (csp_pattern_data + PATOFF_PACK_FIELD)
#define pat_pack (csp_pattern_data + PATOFF_PACK)

// Defined by ONE translation unit -- csp_parse.c, which owns pmatch.
#ifdef CSP_PATTERN_DEFINE

const uint8_t csp_pattern_data[] RODATA = {
    // pat_res @ 0
    P_OPT, 6,
        P_TOK, COLON,
        P_INTEGER_S, csp_offsetof(res_param_t, res), STOP_RES_RES,
        P_OPT_END,
    P_END,
    // pat_port_pin @ 9
    P_CHOICE, 2,
        P_ALT, 9,
            P_INTEGER_S, csp_offsetof(port_pin_t, port), STOP_PORT_PIN_PORT,
            P_TOK, COLON,
            P_INTEGER_S, csp_offsetof(port_pin_t, pin), STOP_PORT_PIN_PIN,
            P_ALT_END,
        P_ALT, 4,
            P_INTEGER_S, csp_offsetof(port_pin_t, pin), STOP_PORT_PIN_PIN_2,
            P_ALT_END,
    P_CHOICE_END,
    P_OPT, 3,
        P_TOK, COMMA,
        P_OPT_END,
    P_END,
    // pat_initval @ 35
    P_CONST_S, csp_offsetof(initval_param_t, opts), csp_offsetof(initval_param_t, init), STOP_INITVAL_INIT,
    P_CHOICE, 2,
        P_ALT, 4,
            P_TOK_W, COMMA, csp_offsetof(initval_param_t, sep),
            P_ALT_END,
        P_ALT, 4,
            P_TOK_W, RBRACE, csp_offsetof(initval_param_t, sep),
            P_ALT_END,
    P_CHOICE_END,
    P_END,
    // pat_pin_item @ 55
    P_CHOICE, 2,
        P_ALT, 32,
            P_INTEGER_S, csp_offsetof(pin_item_t, num), STOP_PIN_ITEM_NUM,
            P_OPT, 18,
                P_TOK_W, COLON, csp_offsetof(pin_item_t, colon),
                P_OPT, 12,
                    P_INTEGER_S, csp_offsetof(pin_item_t, lo), STOP_PIN_ITEM_LO,
                    P_OPT, 6,
                        P_TOK, DOTDOT,
                        P_INTEGER_S, csp_offsetof(pin_item_t, hi), STOP_PIN_ITEM_HI,
                        P_OPT_END,
                    P_OPT_END,
                P_OPT_END,
            P_OPT, 6,
                P_TOK, DOTDOT,
                P_INTEGER_S, csp_offsetof(pin_item_t, hi), STOP_PIN_ITEM_HI_2,
                P_OPT_END,
            P_ALT_END,
        P_ALT, 6,
            P_TOK, DOTDOT,
            P_INTEGER_S, csp_offsetof(pin_item_t, hi), STOP_PIN_ITEM_HI_3,
            P_ALT_END,
    P_CHOICE_END,
    P_OPT, 4,
        P_TOK_W, COMMA, csp_offsetof(pin_item_t, sep),
        P_OPT_END,
    P_END,
    // pat_module @ 107
    P_STR, csp_offsetof(module_param_t, name),
    P_END,
    // pat_end @ 110
    P_END,
    // pat_variable @ 111
    P_STR, csp_offsetof(variable_param_t, name),
    P_PAT, (PATOFF_RES >> 8), (PATOFF_RES & 0xff), csp_offsetof(variable_param_t, r), STOP_VARIABLE_RES_CONT,
    P_OPTS, csp_offsetof(variable_param_t, opts),
    P_OPT, 19,
        P_TOK, EQ,
        P_CHOICE, 2,
            P_ALT, 5,
                P_CONST_S, csp_offsetof(variable_param_t, opts), csp_offsetof(variable_param_t, init), STOP_VARIABLE_INIT,
                P_ALT_END,
            P_ALT, 4,
                P_EXPR_S, csp_offsetof(variable_param_t, ini), STOP_VARIABLE_INI,
                P_ALT_END,
        P_CHOICE_END,
        P_OPT_END,
    P_END,
    // pat_constant @ 142
    P_STR, csp_offsetof(constant_param_t, name),
    P_PAT, (PATOFF_RES >> 8), (PATOFF_RES & 0xff), csp_offsetof(constant_param_t, r), STOP_CONSTANT_RES_CONT,
    P_OPTS, csp_offsetof(constant_param_t, opts),
    P_TOK, EQ,
    P_CHOICE, 2,
        P_ALT, 4,
            P_TOK_W, LBRACE, csp_offsetof(constant_param_t, list),
            P_ALT_END,
        P_ALT, 5,
            P_CONST_S, csp_offsetof(constant_param_t, opts), csp_offsetof(constant_param_t, init), STOP_CONSTANT_INIT,
            P_ALT_END,
    P_CHOICE_END,
    P_END,
    // pat_digital @ 170
    P_STR, csp_offsetof(digital_param_t, name),
    P_OPTS, csp_offsetof(digital_param_t, opts),
    P_PAT, (PATOFF_PORT_PIN >> 8), (PATOFF_PORT_PIN & 0xff), csp_offsetof(digital_param_t, port_pin), STOP_DIGITAL_PORT_PIN_CONT,
    P_END,
    // pat_analog @ 180
    P_STR, csp_offsetof(analog_param_t, name),
    P_PAT, (PATOFF_RES >> 8), (PATOFF_RES & 0xff), csp_offsetof(analog_param_t, r), STOP_ANALOG_RES_CONT,
    P_OPTS, csp_offsetof(analog_param_t, opts),
    P_PAT, (PATOFF_PORT_PIN >> 8), (PATOFF_PORT_PIN & 0xff), csp_offsetof(analog_param_t, port_pin), STOP_ANALOG_PORT_PIN_CONT,
    P_END,
    // pat_timer @ 195
    P_STR, csp_offsetof(timer_param_t, name),
    P_CHOICE, 2,
        P_ALT, 4,
            P_INTEGER_S, csp_offsetof(timer_param_t, timeout), STOP_TIMER_TIMEOUT,
            P_ALT_END,
        P_ALT, 4,
            P_EXPR_S, csp_offsetof(timer_param_t, period), STOP_TIMER_PERIOD,
            P_ALT_END,
    P_CHOICE_END,
    P_OPT, 6,
        P_TOK, EQ,
        P_INTEGER_S, csp_offsetof(timer_param_t, init), STOP_TIMER_INIT,
        P_OPT_END,
    P_END,
    // pat_field_decl @ 221
    P_STR, csp_offsetof(field_param_t, name),
    P_PAT, (PATOFF_RES >> 8), (PATOFF_RES & 0xff), csp_offsetof(field_param_t, r), STOP_FIELD_DECL_RES_CONT,
    P_OPTS, csp_offsetof(field_param_t, opts),
    P_STR, csp_offsetof(field_param_t, frame),
    P_TOK, LB,
    P_CHOICE, 2,
        P_ALT, 9,
            P_INTEGER_S, csp_offsetof(field_param_t, bit0), STOP_FIELD_DECL_BIT0,
            P_TOK, DOTDOT,
            P_INTEGER_S, csp_offsetof(field_param_t, bit1), STOP_FIELD_DECL_BIT1,
            P_ALT_END,
        P_ALT, 4,
            P_INTEGER_S, csp_offsetof(field_param_t, bit0), STOP_FIELD_DECL_BIT0_2,
            P_ALT_END,
    P_CHOICE_END,
    P_TOK, RB,
    P_END,
    // pat_buffer @ 257
    P_STR, csp_offsetof(buffer_param_t, name),
    P_PAT, (PATOFF_RES >> 8), (PATOFF_RES & 0xff), csp_offsetof(buffer_param_t, r), STOP_BUFFER_RES_CONT,
    P_OPTS, csp_offsetof(buffer_param_t, opts),
    P_OPT, 6,
        P_TOK, T_CAN,
        P_INTEGER_S, csp_offsetof(buffer_param_t, frameid), STOP_BUFFER_FRAMEID,
        P_OPT_END,
    P_OPT, 12,
        P_TOK, T_I2C,
        P_INTEGER_S, csp_offsetof(buffer_param_t, i2c_bus), STOP_BUFFER_I2C_BUS,
        P_INTEGER_S, csp_offsetof(buffer_param_t, i2c_addr), STOP_BUFFER_I2C_ADDR,
        P_INTEGER_S, csp_offsetof(buffer_param_t, i2c_reg), STOP_BUFFER_I2C_REG,
        P_OPT_END,
    P_OPT, 14,
        P_TOK, T_SPI,
        P_INTEGER_S, csp_offsetof(buffer_param_t, spi_bus), STOP_BUFFER_SPI_BUS,
        P_PAT, (PATOFF_PORT_PIN >> 8), (PATOFF_PORT_PIN & 0xff), csp_offsetof(buffer_param_t, spi_cs), STOP_BUFFER_PORT_PIN_CONT,
        P_INTEGER_S, csp_offsetof(buffer_param_t, spi_cmd), STOP_BUFFER_SPI_CMD,
        P_OPT_END,
    P_OPT, 12,
        P_TOK, T_UDP,
        P_INTEGER_S, csp_offsetof(buffer_param_t, udp_port), STOP_BUFFER_UDP_PORT,
        P_OPT, 4,
            P_INTEGER_S, csp_offsetof(buffer_param_t, udp_ip), STOP_BUFFER_UDP_IP,
            P_OPT_END,
        P_OPT_END,
    P_END,
    // pat_body @ 319
    P_OPT, 63,
        P_STR, csp_offsetof(rule_body_part_t, obj),
        P_OPT, 12,
            P_TOK, DOT,
            P_STR, csp_offsetof(rule_body_part_t, fld),
            P_OPT, 5,
                P_TOK, DOT,
                P_STR, csp_offsetof(rule_body_part_t, pfld),
                P_OPT_END,
            P_OPT_END,
        P_OPT, 18,
            P_TOK_W, LB, csp_offsetof(rule_body_part_t, has_idx),
            P_INTEGER_S, csp_offsetof(rule_body_part_t, idx0), STOP_BODY_IDX0,
            P_OPT, 7,
                P_TOK_W, DOTDOT, csp_offsetof(rule_body_part_t, has_range),
                P_INTEGER_S, csp_offsetof(rule_body_part_t, idx1), STOP_BODY_IDX1,
                P_OPT_END,
            P_TOK, RB,
            P_OPT_END,
        P_OPT, 9,
            P_TOK_W, LB, csp_offsetof(rule_body_part_t, has_idx),
            P_EXPR_S, csp_offsetof(rule_body_part_t, idxe), STOP_BODY_IDXE,
            P_TOK, RB,
            P_OPT_END,
        P_CHOICE, 2,
            P_ALT, 4,
                P_TOK_W, EQ, csp_offsetof(rule_body_part_t, assign),
                P_ALT_END,
            P_ALT, 4,
                P_TOK_W, RIMP, csp_offsetof(rule_body_part_t, assign),
                P_ALT_END,
        P_CHOICE_END,
        P_OPT_END,
    P_EXPR_S, csp_offsetof(rule_body_part_t, rhs), STOP_BODY_RHS,
    P_END,
    // pat_rule @ 388
    P_PAT, (PATOFF_BODY >> 8), (PATOFF_BODY & 0xff), csp_offsetof(rule_param_t, body), STOP_RULE_BODY_CONT,
    P_REP, 11,
        P_ARRAY, csp_offsetof(rule_param_t, body[1]), sizeof(rule_body_part_t),
        P_TOK, COMMA,
        P_PAT, (PATOFF_BODY >> 8), (PATOFF_BODY & 0xff), 0, STOP_RULE_BODY_CONT,
        P_REP_END,
    P_OPT, 6,
        P_TOK, QUEST,
        P_EXPR_S, csp_offsetof(rule_param_t, cond), STOP_RULE_COND,
        P_OPT_END,
    P_END,
    // pat_object @ 415
    P_STR, csp_offsetof(object_param_t, mod_name),
    P_STR, csp_offsetof(object_param_t, obj_name),
    P_OPT, 12,
        P_REP, 9,
            P_ARRAY, csp_offsetof(object_param_t, inits), sizeof(rule_body_part_t),
            P_PAT, (PATOFF_BODY >> 8), (PATOFF_BODY & 0xff), 0, STOP_OBJECT_BODY_CONT,
            P_REP_END,
        P_OPT_END,
    P_END,
    // pat_pack_field @ 434
    P_EXPR_S, csp_offsetof(pack_field_t, val), STOP_PACK_FIELD_VAL,
    P_OPT, 6,
        P_TOK, COLON,
        P_INTEGER_S, csp_offsetof(pack_field_t, bits), STOP_PACK_FIELD_BITS,
        P_OPT_END,
    P_END,
    // pat_pack @ 446
    P_STR, csp_offsetof(pack_param_t, buffer),
    P_CHOICE, 2,
        P_ALT, 4,
            P_TOK_W, LTLT, csp_offsetof(pack_param_t, op),
            P_ALT_END,
        P_ALT, 4,
            P_TOK_W, GTGT, csp_offsetof(pack_param_t, op),
            P_ALT_END,
    P_CHOICE_END,
    P_TOK, EQ,
    P_REP, 9,
        P_ARRAY, csp_offsetof(pack_param_t, field), sizeof(pack_field_t),
        P_PAT, (PATOFF_PACK_FIELD >> 8), (PATOFF_PACK_FIELD & 0xff), 0, STOP_PACK_PACK_FIELD_CONT,
        P_REP_END,
    P_OPT, 6,
        P_TOK, QUEST,
        P_EXPR_S, csp_offsetof(pack_param_t, cond), STOP_PACK_COND,
        P_OPT_END,
    P_END,
};

#endif

#endif
